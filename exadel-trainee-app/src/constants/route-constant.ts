export enum Route {
  users = 'users',
  home = 'home',
  check = 'check',
  editor = 'editor',
  manage = 'manage',
  statistics = 'statistics',
}

export enum Role {
  user = 'user',
  hr = 'hr',
  couch = 'couch',
  admin = 'admin',
}

export enum SidebarMenu {
  home = 'Home',
  users = 'Users',
  check = 'Check',
  editor = 'Editor',
  manage = 'Manage',
  statistics = 'Statistics',
}

export enum UserViewValue {
  user = 'User',
  hr = 'HR',
  couch = 'Couch',
  admin = 'Admin',
}

export enum Level {
  beginner = 'beginner',
  intermediate = 'intermediate',
  advanced = 'advanced',
}
